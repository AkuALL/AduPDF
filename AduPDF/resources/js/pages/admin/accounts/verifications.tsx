import { Head, Link, router } from '@inertiajs/react';
import AdminLayout from '@/layouts/admin-layout';

type User = { id: number; nama?: string; name?: string; email: string; created_at: string | null; verification_status: 'pending' | 'approved' | 'rejected' };
type Props = { users: { data: User[]; links?: { url: string | null; label: string; active: boolean }[] }; status: 'pending' | 'approved' | 'rejected'; counts: Record<'pending' | 'approved' | 'rejected', number> };
const labels = { pending: 'Menunggu Verifikasi', approved: 'Disetujui', rejected: 'Ditolak' };

export default function Verifications({ users, status, counts }: Props) {
    function process(user: User, action: 'verify' | 'reject') {
        const verb = action === 'verify' ? 'MENYETUJUI' : 'MENOLAK';
        if (window.confirm(`Apakah Anda yakin ingin ${verb} akun ${user.nama ?? user.name}?${action === 'reject' ? ' Akun tidak akan dapat login.' : ''}`)) router.patch(`/admin/users/${user.id}/${action}`);
    }
    return <AdminLayout><Head title="Antrian Verifikasi Pengguna" /><div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-4 border-b border-slate-200 p-6 md:flex-row md:items-center md:justify-between"><div><h1 className="text-xl font-bold">Verifikasi Akun</h1><p className="mt-1 text-xs text-slate-500">Tinjau dan proses akun yang menunggu persetujuan.</p></div><div className="flex gap-2"><Link href="/admin/users/pengguna/create" className="rounded-md border border-slate-300 bg-slate-100 px-3 py-2 text-xs font-medium">+ Buat Pengguna Langsung</Link><Link href="/admin/users/petugas/create" className="rounded-md bg-[#2D4C79] px-3 py-2 text-xs font-medium text-white">+ Buat Akun Petugas</Link></div></div><div className="flex border-b border-slate-200 bg-slate-50 px-6">{(Object.keys(labels) as Array<keyof typeof labels>).map((key) => <Link key={key} href={`/admin/verifications?status=${key}`} className={`flex items-center gap-2 border-b-2 px-4 py-3 text-xs font-medium ${status === key ? 'border-[#2D4C79] bg-white font-bold text-[#2D4C79]' : 'border-transparent text-slate-600'}`}>{labels[key]} <span className="rounded-full bg-slate-200 px-2 py-0.5 text-[10px] text-slate-700">{counts[key]}</span></Link>)}</div><div className="overflow-x-auto"><table className="w-full text-left text-sm text-slate-700"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-6 py-3">Nama Lengkap</th><th className="px-6 py-3">Alamat Email</th><th className="px-6 py-3">Tanggal Daftar</th><th className="px-6 py-3">Status</th><th className="px-6 py-3 text-right">Aksi</th></tr></thead><tbody className="divide-y divide-slate-200">{users.data.length === 0 ? <tr><td colSpan={5} className="px-6 py-12 text-center text-sm text-slate-500">Tidak ada data untuk status ini</td></tr> : users.data.map((user) => <tr key={user.id} className="hover:bg-slate-50"><td className="px-6 py-4 font-medium text-slate-900">{user.nama ?? user.name}</td><td className="px-6 py-4">{user.email}</td><td className="px-6 py-4 text-xs text-slate-500">{user.created_at ? `${new Date(user.created_at).toLocaleString('id-ID')} WIB` : '-'}</td><td className="px-6 py-4"><span className="rounded-full border bg-slate-100 px-2.5 py-1 text-xs font-medium">{labels[user.verification_status]}</span></td><td className="space-x-2 px-6 py-4 text-right">{user.verification_status === 'pending' ? <><button onClick={() => process(user, 'verify')} className="rounded bg-emerald-600 px-3 py-1.5 text-xs font-medium text-white">Setujui</button><button onClick={() => process(user, 'reject')} className="rounded bg-rose-600 px-3 py-1.5 text-xs font-medium text-white">Tolak</button></> : <span className="text-xs italic text-slate-400">Sudah Diproses</span>}</td></tr>)}</tbody></table></div>{users.links && <div className="flex gap-1 border-t border-slate-200 p-4">{users.links.map((link) => link.url && <Link key={link.label} href={link.url} preserveScroll className={`rounded px-3 py-1 text-xs ${link.active ? 'bg-[#2D4C79] text-white' : 'bg-slate-100'}`} dangerouslySetInnerHTML={{ __html: link.label }} />)}</div>}</div></AdminLayout>;
}
